<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tje.yakssok">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".board.Board_MainActivity" />
        <activity android:name=".board.Board_SelectedActivity" />
        <activity android:name=".board.Board_ViewActivity" />
        <activity android:name=".board.Board_WriteActivity" />
        <activity android:name=".member.JoinActivity" />
        <activity android:name=".member.DaumWebViewActivity" />
        <activity android:name=".member.Modify_Pw_Activity" />
        <activity android:name=".member.ProfileModifyActivity" />
        <activity android:name=".pill.Pill_ListActivity"></activity>
    </application>

</manifest>